Svelte-Template

In terminal
Run "yarn install" (to add node_modules.)

If yarn isn't installed
Run "npm i yarn -g" (i for install, -g for install globaly)

If execution policy error run the following 
PowerShell.exe -ExecutionPolicy Bypass -File C:\Users\Ray\AppData\Roaming\npm\yarn.ps1 or 
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope LocalMachine

To Run: http://localhost:5500/builds/